# Merlin Grounding

**Authoritative grounding, sanity anchors, and project state for Merlin**

This repository exists as the **single source of project truth** for the Merlin assistant system. It is designed to document and anchor the current state of the project — including decisions, configuration, sanity checks, and overall progress — so that context does not drift over time.

## 🧠 Purpose

Merlin is an AI assistant project with:
- a persistent brain process running independently of web server lifecycles
- a text/voice chat UI
- a stability-first execution model
- explicit grounding and sanity anchors

This repo is **NOT** for runtime secrets, API keys, passwords, or sensitive credentials.

## 📦 How This Repo Works

The repository contains:

